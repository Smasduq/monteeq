[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--version" # Optional: ensure npm version check

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
